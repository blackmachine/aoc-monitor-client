

fn update(events) { 
    
    // log("Yay.");


    set_buffer("main_buf");
    set_cursor(1, 28);
    set_fg(0, 255, 127, 255);
    write_text("ALL OF CREATE\n\n91.214.241.217");

    set_cursor(30, 30);
    set_fg(0, 255, 127, 255);
    write_text("a");
    // log("Out");
    
}